apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "empty-chart.fullname" . }}-test-connection"
  labels:
    {{- include "empty-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "empty-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
