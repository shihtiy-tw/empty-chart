apiVersion: v1
kind: Service
metadata:
  name: {{ include "empty-chart.fullname" . }}
  labels:
    {{- include "empty-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "empty-chart.selectorLabels" . | nindent 4 }}
